Create nw- (project NW-Cust), works fine
Copy these files (customizations from nw sample)
Run Admin -> Swagger -> ServicesEndPoint/add_order - it fails (nw sample works fine)

The failure is in safrs_api@668: AttributeError("'Employee' object has no attribute 'Manager'")